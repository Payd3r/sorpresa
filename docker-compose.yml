version: '3.8'
services:
  frontend:
    build: .
    container_name: sorpresa-frontend
    volumes:
      - .:/app
      - /app/node_modules 
    networks:
      - web-proxy

networks:
  web-proxy:
    external: true